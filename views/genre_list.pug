extends layout

block content
  h1= title
  

  div(class="col-md-8")

  
  ul(class="list-group")
  each val in list_genres
    li(class="list-group-item")
      a(href=val.url) #{val.name}

  else
    li There are no genre.
